{% extends 'layouts/admin/base.twig' %}

{% block title %}Tijdsloten{% endblock %}

{% block content %}
<div class="flex justify-between items-center mb-6">
    <h2 class="text-2xl font-bold text-gray-800">Tijdsloten</h2>
</div>

<!-- Filter -->
<div class="bg-white rounded-lg shadow p-4 mb-6">
    <form method="GET" action="/admin/appointment-slots" class="flex items-center gap-4">
        <label class="text-sm font-medium text-gray-700">Filter op type:</label>
        <select name="type_id" onchange="this.form.submit()"
                class="border border-gray-300 rounded-lg px-3 py-2 text-sm focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
            <option value="">Alle types</option>
            {% for t in types %}
                <option value="{{ t.id }}" {{ selected_type_id == t.id ? 'selected' : '' }}>{{ t.name_nl }}</option>
            {% endfor %}
        </select>
    </form>
</div>

<!-- Add new slot -->
<div class="bg-white rounded-lg shadow p-4 mb-6" x-data="{ showForm: false }">
    <div class="flex justify-between items-center">
        <h3 class="font-semibold text-gray-800">Nieuw tijdslot</h3>
        <button @click="showForm = !showForm" class="text-sm text-blue-600 hover:text-blue-800 font-medium"
                x-text="showForm ? 'Sluiten' : '+ Toevoegen'"></button>
    </div>
    <div x-show="showForm" x-cloak class="mt-4">
        <form action="/admin/appointment-slots/store" method="POST" class="flex items-end gap-3 flex-wrap">
            {{ csrf_field|raw }}
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Type</label>
                <select name="appointment_type_id" required
                        class="border border-gray-300 rounded-lg px-3 py-2 text-sm focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                    {% for t in types %}
                        <option value="{{ t.id }}" {{ selected_type_id == t.id ? 'selected' : '' }}>{{ t.name_nl }}</option>
                    {% endfor %}
                </select>
            </div>
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Dag</label>
                <select name="day_of_week" required
                        class="border border-gray-300 rounded-lg px-3 py-2 text-sm focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                    <option value="0">Zondag</option>
                    <option value="1">Maandag</option>
                    <option value="2">Dinsdag</option>
                    <option value="3">Woensdag</option>
                    <option value="4">Donderdag</option>
                    <option value="5">Vrijdag</option>
                    <option value="6" selected>Zaterdag</option>
                </select>
            </div>
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Start</label>
                <input type="time" name="start_time" required
                       class="border border-gray-300 rounded-lg px-3 py-2 text-sm focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
            </div>
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Einde</label>
                <input type="time" name="end_time" required
                       class="border border-gray-300 rounded-lg px-3 py-2 text-sm focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
            </div>
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Max boekingen</label>
                <input type="number" name="max_bookings" value="1" min="1"
                       class="w-20 border border-gray-300 rounded-lg px-3 py-2 text-sm focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
            </div>
            <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg text-sm font-medium transition">
                Toevoegen
            </button>
        </form>
    </div>
</div>

<!-- Slots list -->
{% set days = ['Zondag', 'Maandag', 'Dinsdag', 'Woensdag', 'Donderdag', 'Vrijdag', 'Zaterdag'] %}
<div class="space-y-3">
    {% for slot in slots %}
    <div class="bg-white rounded-lg shadow p-4" x-data="{ editing: false }">
        {# View mode #}
        <div x-show="!editing" class="flex items-center justify-between">
            <div class="flex items-center gap-6">
                <span class="text-sm font-medium text-gray-900 w-40">{{ slot.type_name ?? slot.type }}</span>
                <span class="text-sm text-gray-600 w-24">{{ days[slot.day_of_week] }}</span>
                <span class="text-sm font-mono text-gray-800">{{ slot.start_time[:5] }} - {{ slot.end_time[:5] }}</span>
                <span class="text-xs text-gray-500">max {{ slot.max_bookings }}</span>
                <span class="px-2 py-1 text-xs rounded-full {{ slot.is_active ? 'bg-green-100 text-green-800' : 'bg-gray-100 text-gray-600' }}">
                    {{ slot.is_active ? 'Actief' : 'Inactief' }}
                </span>
            </div>
            <div class="flex items-center gap-2">
                <button @click="editing = true" class="text-blue-600 hover:text-blue-800 text-sm font-medium">Bewerken</button>
                <form action="/admin/appointment-slots/{{ slot.id }}/delete" method="POST" class="inline" onsubmit="return confirm('Tijdslot verwijderen?')">
                    {{ csrf_field|raw }}
                    <button type="submit" class="text-red-600 hover:text-red-800 text-sm font-medium">Verwijderen</button>
                </form>
            </div>
        </div>

        {# Edit mode #}
        <form x-show="editing" x-cloak action="/admin/appointment-slots/{{ slot.id }}/update" method="POST" class="flex items-end gap-3 flex-wrap">
            {{ csrf_field|raw }}
            <div>
                <label class="block text-xs font-medium text-gray-500 mb-1">Dag</label>
                <select name="day_of_week" class="border border-gray-300 rounded px-2 py-1.5 text-sm">
                    {% for i in 0..6 %}
                        <option value="{{ i }}" {{ slot.day_of_week == i ? 'selected' : '' }}>{{ days[i] }}</option>
                    {% endfor %}
                </select>
            </div>
            <div>
                <label class="block text-xs font-medium text-gray-500 mb-1">Start</label>
                <input type="time" name="start_time" value="{{ slot.start_time[:5] }}"
                       class="border border-gray-300 rounded px-2 py-1.5 text-sm w-28">
            </div>
            <div>
                <label class="block text-xs font-medium text-gray-500 mb-1">Einde</label>
                <input type="time" name="end_time" value="{{ slot.end_time[:5] }}"
                       class="border border-gray-300 rounded px-2 py-1.5 text-sm w-28">
            </div>
            <div>
                <label class="block text-xs font-medium text-gray-500 mb-1">Max</label>
                <input type="number" name="max_bookings" value="{{ slot.max_bookings }}" min="1"
                       class="border border-gray-300 rounded px-2 py-1.5 text-sm w-16">
            </div>
            <div>
                <label class="inline-flex items-center gap-1">
                    <input type="hidden" name="is_active" value="0">
                    <input type="checkbox" name="is_active" value="1" {{ slot.is_active ? 'checked' : '' }}
                           class="rounded border-gray-300 text-blue-600">
                    <span class="text-sm text-gray-700">Actief</span>
                </label>
            </div>
            <button type="submit" class="bg-green-600 hover:bg-green-700 text-white px-4 py-1.5 rounded text-sm font-medium">Opslaan</button>
            <button type="button" @click="editing = false" class="text-gray-500 hover:text-gray-700 text-sm font-medium">Annuleren</button>
        </form>
    </div>
    {% else %}
    <div class="bg-white rounded-lg shadow p-8 text-center text-gray-500">Geen tijdsloten gevonden.</div>
    {% endfor %}
</div>

<p class="text-sm text-gray-500 mt-4">De tijdslot info tekst (boven de tijdsloten voor klanten) kan aangepast worden via <a href="/admin/settings" class="text-blue-600 hover:text-blue-800">Instellingen</a> → Afspraken.</p>
{% endblock %}
