{% extends 'layouts/admin/base.twig' %}

{% block title %}Blok aanmaken{% endblock %}

{% block content %}
<div class="mb-6">
    <a href="/admin/blocks" class="text-blue-600 hover:text-blue-800 text-sm">&larr; Terug naar blokken</a>
</div>

<h2 class="text-2xl font-bold text-gray-800 mb-6">Nieuw blok</h2>

<div class="bg-white rounded-lg shadow p-6 max-w-4xl">
    <form action="/admin/blocks" method="POST" enctype="multipart/form-data" id="blockForm">
        {{ csrf_field|raw }}

        <div class="space-y-4">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                <div>
                    <label for="title" class="block text-sm font-medium text-gray-700 mb-1">Titel</label>
                    <input type="text" name="title" id="title" required
                           class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
                           value="{{ old.title }}">
                </div>

                <div>
                    <label for="subtitle" class="block text-sm font-medium text-gray-700 mb-1">Ondertitel</label>
                    <input type="text" name="subtitle" id="subtitle"
                           class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
                           value="{{ old.subtitle }}" placeholder="Optionele ondertitel of label">
                </div>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                <div>
                    <label for="type" class="block text-sm font-medium text-gray-700 mb-1">Type</label>
                    <select name="type" id="type" required
                            class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                        <option value="hero" {{ old.type == 'hero' ? 'selected' : '' }}>Hero (hoofdbanner)</option>
                        <option value="feature" {{ old.type == 'feature' ? 'selected' : '' }}>Feature (dienst-kaart)</option>
                        <option value="gallery" {{ old.type == 'gallery' ? 'selected' : '' }}>Gallery (portfolio-item)</option>
                        <option value="text" {{ old.type == 'text' ? 'selected' : '' }}>Text (over ons)</option>
                        <option value="cta" {{ old.type == 'cta' ? 'selected' : '' }}>CTA (call-to-action)</option>
                    </select>
                </div>

                <div>
                    <label for="site_id" class="block text-sm font-medium text-gray-700 mb-1">Site</label>
                    <select name="site_id" id="site_id" required
                            class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                        <option value="">Selecteer site</option>
                        {% for s in sites %}
                            <option value="{{ s.id }}" {{ old.site_id == s.id ? 'selected' : '' }}>{{ s.name }}</option>
                        {% endfor %}
                    </select>
                </div>

                <div>
                    <label for="link_url" class="block text-sm font-medium text-gray-700 mb-1">Link URL</label>
                    <input type="text" name="link_url" id="link_url"
                           class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
                           value="{{ old.link_url }}" placeholder="/pagina-slug of https://...">
                </div>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                <div>
                    <label for="image" class="block text-sm font-medium text-gray-700 mb-1">Afbeelding uploaden</label>
                    <input type="file" name="image" id="image" accept="image/*"
                           class="w-full border border-gray-300 rounded-lg px-3 py-2 text-sm file:mr-3 file:py-1 file:px-3 file:rounded file:border-0 file:bg-blue-50 file:text-blue-700 file:font-medium hover:file:bg-blue-100">
                </div>

                <div>
                    <label for="image_url" class="block text-sm font-medium text-gray-700 mb-1">Of afbeelding URL</label>
                    <input type="url" name="image_url" id="image_url"
                           class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
                           value="{{ old.image_url }}" placeholder="https://images.unsplash.com/...">
                    <p class="text-xs text-gray-500 mt-1">Upload heeft voorrang op URL.</p>
                </div>
            </div>

            <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Inhoud</label>
                <div id="editor" class="bg-white" style="min-height: 250px;">{{ old.content|raw }}</div>
                <textarea name="content" id="content" class="hidden">{{ old.content }}</textarea>
            </div>
        </div>

        <div class="mt-6 flex gap-3">
            <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white px-6 py-2 rounded-lg text-sm font-medium transition">
                Opslaan
            </button>
            <a href="/admin/blocks" class="bg-gray-200 hover:bg-gray-300 text-gray-700 px-6 py-2 rounded-lg text-sm font-medium transition">
                Annuleren
            </a>
        </div>
    </form>
</div>
{% endblock %}

{% block scripts %}
<script>
    var quill = new Quill('#editor', {
        theme: 'snow',
        modules: {
            toolbar: [
                [{ 'header': [1, 2, 3, false] }],
                ['bold', 'italic', 'underline', 'strike'],
                [{ 'list': 'ordered'}, { 'list': 'bullet' }],
                ['link', 'image'],
                ['clean']
            ]
        }
    });

    document.getElementById('blockForm').addEventListener('submit', function() {
        document.getElementById('content').value = quill.root.innerHTML;
    });
</script>
{% endblock %}
