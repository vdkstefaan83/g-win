{% extends 'layouts/admin/base.twig' %}

{% block title %}Site bewerken{% endblock %}

{% block content %}
<div class="mb-6">
    <a href="/admin/sites" class="text-blue-600 hover:text-blue-800 text-sm">&larr; Terug naar sites</a>
</div>

<h2 class="text-2xl font-bold text-gray-800 mb-6">Site bewerken: {{ site_item.name }}</h2>

<div class="bg-white rounded-lg shadow p-6 max-w-2xl">
    <form action="/admin/sites/{{ site_item.id }}/update" method="POST">
        {{ csrf_field|raw }}

        <div class="space-y-4">
            <div>
                <label for="name" class="block text-sm font-medium text-gray-700 mb-1">Naam</label>
                <input type="text" name="name" id="name" required
                       class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
                       value="{{ site_item.name }}">
            </div>

            <div>
                <label for="slug" class="block text-sm font-medium text-gray-700 mb-1">Slug</label>
                <input type="text" name="slug" id="slug" required
                       class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
                       value="{{ site_item.slug }}">
            </div>

            <div>
                <label for="domain" class="block text-sm font-medium text-gray-700 mb-1">Domein</label>
                <input type="text" name="domain" id="domain"
                       class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
                       value="{{ site_item.domain }}" placeholder="voorbeeld.nl">
            </div>

            <div>
                <label for="layout" class="block text-sm font-medium text-gray-700 mb-1">Layout</label>
                <select name="layout" id="layout"
                        class="w-full border border-gray-300 rounded-lg px-3 py-2 focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                    <option value="default" {{ site_item.layout == 'default' ? 'selected' : '' }}>Default</option>
                    <option value="gwin" {{ site_item.layout == 'gwin' ? 'selected' : '' }}>G-Win</option>
                    <option value="shop" {{ site_item.layout == 'shop' ? 'selected' : '' }}>Shop</option>
                </select>
            </div>
        </div>

        <div class="mt-6 flex gap-3">
            <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white px-6 py-2 rounded-lg text-sm font-medium transition">
                Bijwerken
            </button>
            <a href="/admin/sites" class="bg-gray-200 hover:bg-gray-300 text-gray-700 px-6 py-2 rounded-lg text-sm font-medium transition">
                Annuleren
            </a>
        </div>
    </form>
</div>
{% endblock %}
